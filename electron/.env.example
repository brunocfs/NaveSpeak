# Copie para electron/.env
# URL do servidor NaveSpeak que este app vai carregar - a mesma URL que os
# usuários acessariam pelo navegador. Dentro da VPN (ex.: RadminVPN), aponte
# para o IP da VPN atribuído à máquina que hospeda o servidor.
NAVESPEAK_SERVER_URL=http://localhost:4000
